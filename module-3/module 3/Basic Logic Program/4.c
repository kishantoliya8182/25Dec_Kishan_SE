#include<stdio.h>
#include<conio.h>
main()
{
      long int radius;
      long int area;
      printf("Enter of your radius:");
      scanf("%ld",&radius);
      area = radius*radius;
      printf("Area of square is: %ld", area);
      getch();
      }
