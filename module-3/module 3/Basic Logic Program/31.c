#include<stdio.h>
main()
{
      long int km;
      long int meters;
      printf("Enter Kilometers:");
      scanf("%ld",&km);
      meters = km*1000;
      printf("Your Kilometers TO meters values is:%ld",meters);
      }
