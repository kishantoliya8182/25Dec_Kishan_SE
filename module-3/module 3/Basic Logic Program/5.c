#include<stdio.h>
#include<conio.h>
main()
{
      long int radius;
      long int area;
      printf("Enter cude radius:");
      scanf("%ld",&radius);
      area = 6*radius*radius;
      printf("Area of cube is:%ld", area);
      getch();
      }
