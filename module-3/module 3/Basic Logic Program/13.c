#include<stdio.h>
#include<conio.h>
main()
{
      char value;
      printf("Enter one Character:");
      scanf("%c",&value);
      printf("Your character ascii value is:%d",value);
      getch();
      }
