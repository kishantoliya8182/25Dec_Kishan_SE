#include<stdio.h>
#include<conio.h>
main()
{      
      char countryname[30];
      printf("Enter country name:");
      scanf("%s",&countryname);
      printf("Country name in abbreviate form is = %.3s",countryname);
      getch();
      }
