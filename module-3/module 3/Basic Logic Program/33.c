#include<stdio.h>
#include<math.h>
main(){
	int a,b,c,num;
	
	printf("Enter number:");
	scanf("%d",&num);
	
	a = pow(num,1);
	b = pow(num,2);
	c = pow(num,3);
	
	printf("Ans is:%d %d %d ",a,b,c);
	
  }
