#include<stdio.h>
main()
{
 	  int a;
      int b;
      printf("Enter number of a:");
      scanf("%d",&a);
      printf("Enter number of b:");
      scanf("%d",&b);
      
      if(a==b)
      {
           printf("A and B are same");    
      }else
      {
           printf("A and B are not same"); 
      }
}
